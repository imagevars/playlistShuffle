import React from "react";
import { Container, Heading, Flex } from "@chakra-ui/react";

function Loading() {
  return (
    <Container>
      <Flex>
        <Heading>The item is Loading</Heading>
      </Flex>
    </Container>
  );
}

export default Loading;
